udemy [現役シリコンバレーエンジニアが教えるGo入門＋応用でビットコインのシストレFintechアプリの開発]

第６回　Hello World
//第４回はWindowsの環境構築、第５回は資料共有のみのためスルー
//codeは別ファイルにて管理（どうせあげられないし）

ハローワールドやってみよう：
    早速goland便利やなー。予測の表示もさることながら、必要なパッケージのimport文を自動入力とは。。。
    vscodeでも同じようにできるけどね。

    まず、GoはFunc main()を一番最初に読み込んで実行する。
    他の関数を書いた時は、main関数内で関数の呼び出しをすることになる。

    ただし、init関数を使った場合は、mainより先（一番最初）に実行させることができる。
    init関数は初期設定などをする場合に活用されることが多いらしい。

----
